name: Build and deploy ventas backend

on:
  push:
    branches:
      - deploy
  workflow_dispatch:

env:
  IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/backend-ventas
  SERVICE_NAME: backend-ventas

jobs:
  build-push-deploy:
    name: Build, push and deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Deploy service on EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.BACK_EC2_HOST }}
          username: ${{ secrets.BACK_EC2_USER }}
          key: ${{ secrets.BACK_EC2_SSH_KEY }}
          proxy_host: ${{ secrets.BASTION_EC2_HOST }}
          proxy_username: ${{ secrets.BASTION_EC2_USER }}
          proxy_key: ${{ secrets.BASTION_EC2_SSH_KEY }}
          script: |
            set -e
            cd "${{ secrets.BACK_EC2_DEPLOY_PATH }}"
            docker compose pull ${{ env.SERVICE_NAME }}
            docker compose up -d --no-deps ${{ env.SERVICE_NAME }}
            docker image prune -f
